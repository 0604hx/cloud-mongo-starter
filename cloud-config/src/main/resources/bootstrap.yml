server:
  port: 8900

spring:
  application:
    name: DISS-CONFIG
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:/config

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8901/eureka/
